doctype html
html
  head
    meta(charset='utf-8')
    title Foo
body
h1 #{aname}
ul#users
- each user, name in users
   - if (user.isA == "ferret")
      li(class='user-' + name) #{name} is just a ferret
   - else
      li(class='user-' + name) #{name} #{user.email}